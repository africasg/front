import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
};

export default nextConfig;
// cuando hagamos cambios en servidor y que se renderiza donde lo haremos aqui 
